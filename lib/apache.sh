# Restart apache
alias graceful='sudo apachectl graceful'

# Edit your virtual hosts file.  (For manual virtual host setup)
alias vhosts='sudo mate /etc/apache2/extra/httpd-vhosts.conf'

# Edit your httpd.conf file
alias httpdconf='sudo mate /etc/apache2/httpd.conf'

# Edit you computer's hosts file
alias hosts='mate /etc/hosts'
